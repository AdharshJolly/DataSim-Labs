[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && /opt/venv/bin/python -m honcho -f Procfile start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.healthcheck]
path = "/health"
timeout = 30
