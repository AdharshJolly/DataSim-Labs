[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && /opt/venv/bin/python run_services.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.healthcheck]
path = "/health"
timeout = 30
